[phases.setup]
nixPkgs = ['python311', 'nodejs_20']

[phases.install]
cmds = ['pip install -r requirements.txt', 'npm install']

[phases.build]
cmds = ['echo "Building during start phase..."']

[start]
cmd = 'npm run build && python -m src.backend.api'
