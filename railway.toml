# Railway.app Configuration for Translation QA Tool
# Single service deployment: Frontend + Backend unified

[build]
# Build command: Install dependencies and build React frontend
cmd = "npm install && npm run build && pip install -r requirements.txt"

[start]
# Start command: Run Flask backend server
cmd = "python -m src.backend.api"
